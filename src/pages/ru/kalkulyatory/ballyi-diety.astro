---
import BaseLayout from '@/layouts/BaseLayout.astro';
import WeightWatchersCalculator from '@/components/calculators/WeightWatchersCalculator.astro';
import { t } from '@/utils/i18n';

const title = t('weightWatchers.title', 'ru') + ' - ' + t('site.title', 'ru');
const description = t('weightWatchers.metaDescription', 'ru');
---

<BaseLayout title={title} description={description} lang="ru" calculator="weightWatchers">
  <div class="container">
    <div class="page-header">
      <h1>{t('weightWatchers.title', 'ru')}</h1>
      <p class="page-description">{t('weightWatchers.description', 'ru')}</p>
    </div>

    <WeightWatchersCalculator lang="ru" />
  </div>

  <style>
    .page-header {
      text-align: center;
      padding: 2rem 0;
    }

    .page-header h1 {
      font-size: 2.5rem;
      color: #333;
      margin-bottom: 0.5rem;
    }

    .page-description {
      font-size: 1.25rem;
      color: #666;
    }
  </style>
</BaseLayout>
